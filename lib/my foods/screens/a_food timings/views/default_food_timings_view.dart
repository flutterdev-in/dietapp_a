import 'package:flutter/cupertino.dart';

class DefaultFoodTimingsView extends StatelessWidget {
  const DefaultFoodTimingsView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return StreamBuilder<Object>(
      stream: null,
      builder: (context, snapshot) {
        return Container(
          
        );
      }
    );
  }
}