import 'package:flutter/material.dart';

class CopyScreenFromPlans extends StatelessWidget {
  const CopyScreenFromPlans({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}