import 'package:flutter/material.dart';

class TimingModelScreen extends StatelessWidget {
  const TimingModelScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}